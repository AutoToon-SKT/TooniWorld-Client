import 'package:flutter/material.dart';

class ShowCartoonScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cartoon Screen'),
      ),
      body: Center(
        child: Text('Your cartoon content goes here.'),
      ),
    );
  }
}
